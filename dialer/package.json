{
  "name": "axon-dialer",
  "version": "0.1.0",
  "private": true,
  "description": "Axon outbound dialer worker — scheduler + BullMQ Worker that pulls due campaign_targets and dials via Twilio.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsx src/main.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.0",
    "bullmq": "^5.34.0",
    "ioredis": "^5.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
