livekit-agents[deepgram,openai,silero,turn-detector,anthropic]~=1.5
# Pin MiniMax plugin to >=1.3 explicitly — earlier versions don't expose
# the `audio_format` kwarg we need to bypass the chunked-MP3 decoder bug
# (livekit/livekit#3850, livekit/agents#3863).
livekit-plugins-minimax>=1.3.0
python-dotenv>=1.0.0
supabase>=2.7.0
httpx>=0.27.0
