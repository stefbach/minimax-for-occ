livekit-agents[deepgram,openai,silero,turn-detector,anthropic]~=1.5
# MiniMax plugin pinned to the 1.2.x line: 1.3.0 hard-locks
# livekit-agents==1.2.9 (breaks our 1.5 baseline), while 1.2.9 already
# exposes the `audio_format` kwarg with "pcm" as default, which bypasses
# the chunked-MP3 decoder bug (livekit/livekit#3850, agents#3863).
livekit-plugins-minimax>=1.2.9,<1.3.0
python-dotenv>=1.0.0
supabase>=2.7.0
httpx>=0.27.0
