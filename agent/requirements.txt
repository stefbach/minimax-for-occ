livekit-agents[assemblyai,cartesia,elevenlabs,openai,silero,turn-detector,anthropic,minimax]~=1.5
# assemblyai → livekit-plugins-assemblyai (Universal Streaming STT, <300ms TTFT)
# cartesia   → livekit-plugins-cartesia (Sonic TTS, ~90ms TTFB)
# elevenlabs → livekit-plugins-elevenlabs (Flash v2.5 WebSocket streaming, ~75ms TTFB)
# minimax    → livekit-plugins-minimax-ai (OFFICIAL MiniMax TTS; modern voice
#              catalog Wise_Woman/Calm_Woman/…; handles GroupId internally so it
#              works with just MINIMAX_API_KEY — this is the integration that
#              worked originally, restored 19/06 after the hand-rolled
#              minimax_tts.py (16/06) produced no audio).
python-dotenv>=1.0.0
supabase>=2.7.0
httpx>=0.27.0
# aiohttp : utilisé par agent/replicate_tts.py (preview Wati 15/06) —
# le wrapper TTS Replicate poste en async aux predictions API. Déjà
# transitivement présent via livekit-agents mais on l'explicite.
aiohttp>=3.9.0
